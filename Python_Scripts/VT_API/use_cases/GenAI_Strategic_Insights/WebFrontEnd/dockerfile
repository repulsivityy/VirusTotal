# Dockerfile

# 1. Start from an official Python base image
FROM python:3.10-slim

# 2. Set the working directory inside the container
WORKDIR /app

# 3. Copy the requirements file first
COPY requirements.txt .

# 4. UPGRADE PIP within the container <<<< ADD THIS LINE
RUN pip install --upgrade pip

# 5. Now, install the Python dependencies from requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# 6. Copy your application code into the container
COPY app.py .
COPY report_generator.py .

# 7. Expose the port that Streamlit runs on
EXPOSE 8501

# 8. Define the healthcheck to ensure the app is running
HEALTHCHECK CMD streamlit hello

# 9. Define the command to run when the container starts
CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]